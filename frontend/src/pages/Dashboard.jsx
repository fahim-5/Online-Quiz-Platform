import React from "react";

export default function Dashboard() {
  return (
    <div className="page dashboard-page">
      <h2 className="text-2xl font-bold">Dashboard</h2>
      <p>
        Welcome to your dashboard. Select a quiz to get started. next step: show
        list of quizzes and allow user to select one to take the quiz. next
        step: show quiz details and start quiz button when user selects a quiz.
      </p>
    </div>
  );
}
