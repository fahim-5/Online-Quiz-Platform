# Server (Express + Mongoose)

Install and run:

```bash
cd server
npm install
npm run dev
```

API endpoints start with `/api/`.

Configure database connection in the top-level `.env` file (`MONGO_URI`).
